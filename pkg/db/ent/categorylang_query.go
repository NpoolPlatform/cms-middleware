// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"errors"
	"fmt"
	"math"

	"entgo.io/ent/dialect"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/NpoolPlatform/cms-middleware/pkg/db/ent/categorylang"
	"github.com/NpoolPlatform/cms-middleware/pkg/db/ent/predicate"
)

// CategoryLangQuery is the builder for querying CategoryLang entities.
type CategoryLangQuery struct {
	config
	limit      *int
	offset     *int
	unique     *bool
	order      []OrderFunc
	fields     []string
	predicates []predicate.CategoryLang
	modifiers  []func(*sql.Selector)
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the CategoryLangQuery builder.
func (clq *CategoryLangQuery) Where(ps ...predicate.CategoryLang) *CategoryLangQuery {
	clq.predicates = append(clq.predicates, ps...)
	return clq
}

// Limit adds a limit step to the query.
func (clq *CategoryLangQuery) Limit(limit int) *CategoryLangQuery {
	clq.limit = &limit
	return clq
}

// Offset adds an offset step to the query.
func (clq *CategoryLangQuery) Offset(offset int) *CategoryLangQuery {
	clq.offset = &offset
	return clq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (clq *CategoryLangQuery) Unique(unique bool) *CategoryLangQuery {
	clq.unique = &unique
	return clq
}

// Order adds an order step to the query.
func (clq *CategoryLangQuery) Order(o ...OrderFunc) *CategoryLangQuery {
	clq.order = append(clq.order, o...)
	return clq
}

// First returns the first CategoryLang entity from the query.
// Returns a *NotFoundError when no CategoryLang was found.
func (clq *CategoryLangQuery) First(ctx context.Context) (*CategoryLang, error) {
	nodes, err := clq.Limit(1).All(ctx)
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{categorylang.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (clq *CategoryLangQuery) FirstX(ctx context.Context) *CategoryLang {
	node, err := clq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first CategoryLang ID from the query.
// Returns a *NotFoundError when no CategoryLang ID was found.
func (clq *CategoryLangQuery) FirstID(ctx context.Context) (id uint32, err error) {
	var ids []uint32
	if ids, err = clq.Limit(1).IDs(ctx); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{categorylang.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (clq *CategoryLangQuery) FirstIDX(ctx context.Context) uint32 {
	id, err := clq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single CategoryLang entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one CategoryLang entity is found.
// Returns a *NotFoundError when no CategoryLang entities are found.
func (clq *CategoryLangQuery) Only(ctx context.Context) (*CategoryLang, error) {
	nodes, err := clq.Limit(2).All(ctx)
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{categorylang.Label}
	default:
		return nil, &NotSingularError{categorylang.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (clq *CategoryLangQuery) OnlyX(ctx context.Context) *CategoryLang {
	node, err := clq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only CategoryLang ID in the query.
// Returns a *NotSingularError when more than one CategoryLang ID is found.
// Returns a *NotFoundError when no entities are found.
func (clq *CategoryLangQuery) OnlyID(ctx context.Context) (id uint32, err error) {
	var ids []uint32
	if ids, err = clq.Limit(2).IDs(ctx); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{categorylang.Label}
	default:
		err = &NotSingularError{categorylang.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (clq *CategoryLangQuery) OnlyIDX(ctx context.Context) uint32 {
	id, err := clq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of CategoryLangs.
func (clq *CategoryLangQuery) All(ctx context.Context) ([]*CategoryLang, error) {
	if err := clq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	return clq.sqlAll(ctx)
}

// AllX is like All, but panics if an error occurs.
func (clq *CategoryLangQuery) AllX(ctx context.Context) []*CategoryLang {
	nodes, err := clq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of CategoryLang IDs.
func (clq *CategoryLangQuery) IDs(ctx context.Context) ([]uint32, error) {
	var ids []uint32
	if err := clq.Select(categorylang.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (clq *CategoryLangQuery) IDsX(ctx context.Context) []uint32 {
	ids, err := clq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (clq *CategoryLangQuery) Count(ctx context.Context) (int, error) {
	if err := clq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return clq.sqlCount(ctx)
}

// CountX is like Count, but panics if an error occurs.
func (clq *CategoryLangQuery) CountX(ctx context.Context) int {
	count, err := clq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (clq *CategoryLangQuery) Exist(ctx context.Context) (bool, error) {
	if err := clq.prepareQuery(ctx); err != nil {
		return false, err
	}
	return clq.sqlExist(ctx)
}

// ExistX is like Exist, but panics if an error occurs.
func (clq *CategoryLangQuery) ExistX(ctx context.Context) bool {
	exist, err := clq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the CategoryLangQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (clq *CategoryLangQuery) Clone() *CategoryLangQuery {
	if clq == nil {
		return nil
	}
	return &CategoryLangQuery{
		config:     clq.config,
		limit:      clq.limit,
		offset:     clq.offset,
		order:      append([]OrderFunc{}, clq.order...),
		predicates: append([]predicate.CategoryLang{}, clq.predicates...),
		// clone intermediate query.
		sql:    clq.sql.Clone(),
		path:   clq.path,
		unique: clq.unique,
	}
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		CreatedAt uint32 `json:"created_at,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.CategoryLang.Query().
//		GroupBy(categorylang.FieldCreatedAt).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
//
func (clq *CategoryLangQuery) GroupBy(field string, fields ...string) *CategoryLangGroupBy {
	grbuild := &CategoryLangGroupBy{config: clq.config}
	grbuild.fields = append([]string{field}, fields...)
	grbuild.path = func(ctx context.Context) (prev *sql.Selector, err error) {
		if err := clq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		return clq.sqlQuery(ctx), nil
	}
	grbuild.label = categorylang.Label
	grbuild.flds, grbuild.scan = &grbuild.fields, grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		CreatedAt uint32 `json:"created_at,omitempty"`
//	}
//
//	client.CategoryLang.Query().
//		Select(categorylang.FieldCreatedAt).
//		Scan(ctx, &v)
//
func (clq *CategoryLangQuery) Select(fields ...string) *CategoryLangSelect {
	clq.fields = append(clq.fields, fields...)
	selbuild := &CategoryLangSelect{CategoryLangQuery: clq}
	selbuild.label = categorylang.Label
	selbuild.flds, selbuild.scan = &clq.fields, selbuild.Scan
	return selbuild
}

func (clq *CategoryLangQuery) prepareQuery(ctx context.Context) error {
	for _, f := range clq.fields {
		if !categorylang.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if clq.path != nil {
		prev, err := clq.path(ctx)
		if err != nil {
			return err
		}
		clq.sql = prev
	}
	if categorylang.Policy == nil {
		return errors.New("ent: uninitialized categorylang.Policy (forgotten import ent/runtime?)")
	}
	if err := categorylang.Policy.EvalQuery(ctx, clq); err != nil {
		return err
	}
	return nil
}

func (clq *CategoryLangQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*CategoryLang, error) {
	var (
		nodes = []*CategoryLang{}
		_spec = clq.querySpec()
	)
	_spec.ScanValues = func(columns []string) ([]interface{}, error) {
		return (*CategoryLang).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []interface{}) error {
		node := &CategoryLang{config: clq.config}
		nodes = append(nodes, node)
		return node.assignValues(columns, values)
	}
	if len(clq.modifiers) > 0 {
		_spec.Modifiers = clq.modifiers
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, clq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	return nodes, nil
}

func (clq *CategoryLangQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := clq.querySpec()
	if len(clq.modifiers) > 0 {
		_spec.Modifiers = clq.modifiers
	}
	_spec.Node.Columns = clq.fields
	if len(clq.fields) > 0 {
		_spec.Unique = clq.unique != nil && *clq.unique
	}
	return sqlgraph.CountNodes(ctx, clq.driver, _spec)
}

func (clq *CategoryLangQuery) sqlExist(ctx context.Context) (bool, error) {
	n, err := clq.sqlCount(ctx)
	if err != nil {
		return false, fmt.Errorf("ent: check existence: %w", err)
	}
	return n > 0, nil
}

func (clq *CategoryLangQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := &sqlgraph.QuerySpec{
		Node: &sqlgraph.NodeSpec{
			Table:   categorylang.Table,
			Columns: categorylang.Columns,
			ID: &sqlgraph.FieldSpec{
				Type:   field.TypeUint32,
				Column: categorylang.FieldID,
			},
		},
		From:   clq.sql,
		Unique: true,
	}
	if unique := clq.unique; unique != nil {
		_spec.Unique = *unique
	}
	if fields := clq.fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, categorylang.FieldID)
		for i := range fields {
			if fields[i] != categorylang.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := clq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := clq.limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := clq.offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := clq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (clq *CategoryLangQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(clq.driver.Dialect())
	t1 := builder.Table(categorylang.Table)
	columns := clq.fields
	if len(columns) == 0 {
		columns = categorylang.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if clq.sql != nil {
		selector = clq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if clq.unique != nil && *clq.unique {
		selector.Distinct()
	}
	for _, m := range clq.modifiers {
		m(selector)
	}
	for _, p := range clq.predicates {
		p(selector)
	}
	for _, p := range clq.order {
		p(selector)
	}
	if offset := clq.offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := clq.limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// ForUpdate locks the selected rows against concurrent updates, and prevent them from being
// updated, deleted or "selected ... for update" by other sessions, until the transaction is
// either committed or rolled-back.
func (clq *CategoryLangQuery) ForUpdate(opts ...sql.LockOption) *CategoryLangQuery {
	if clq.driver.Dialect() == dialect.Postgres {
		clq.Unique(false)
	}
	clq.modifiers = append(clq.modifiers, func(s *sql.Selector) {
		s.ForUpdate(opts...)
	})
	return clq
}

// ForShare behaves similarly to ForUpdate, except that it acquires a shared mode lock
// on any rows that are read. Other sessions can read the rows, but cannot modify them
// until your transaction commits.
func (clq *CategoryLangQuery) ForShare(opts ...sql.LockOption) *CategoryLangQuery {
	if clq.driver.Dialect() == dialect.Postgres {
		clq.Unique(false)
	}
	clq.modifiers = append(clq.modifiers, func(s *sql.Selector) {
		s.ForShare(opts...)
	})
	return clq
}

// Modify adds a query modifier for attaching custom logic to queries.
func (clq *CategoryLangQuery) Modify(modifiers ...func(s *sql.Selector)) *CategoryLangSelect {
	clq.modifiers = append(clq.modifiers, modifiers...)
	return clq.Select()
}

// CategoryLangGroupBy is the group-by builder for CategoryLang entities.
type CategoryLangGroupBy struct {
	config
	selector
	fields []string
	fns    []AggregateFunc
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Aggregate adds the given aggregation functions to the group-by query.
func (clgb *CategoryLangGroupBy) Aggregate(fns ...AggregateFunc) *CategoryLangGroupBy {
	clgb.fns = append(clgb.fns, fns...)
	return clgb
}

// Scan applies the group-by query and scans the result into the given value.
func (clgb *CategoryLangGroupBy) Scan(ctx context.Context, v interface{}) error {
	query, err := clgb.path(ctx)
	if err != nil {
		return err
	}
	clgb.sql = query
	return clgb.sqlScan(ctx, v)
}

func (clgb *CategoryLangGroupBy) sqlScan(ctx context.Context, v interface{}) error {
	for _, f := range clgb.fields {
		if !categorylang.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("invalid field %q for group-by", f)}
		}
	}
	selector := clgb.sqlQuery()
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := clgb.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

func (clgb *CategoryLangGroupBy) sqlQuery() *sql.Selector {
	selector := clgb.sql.Select()
	aggregation := make([]string, 0, len(clgb.fns))
	for _, fn := range clgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	// If no columns were selected in a custom aggregation function, the default
	// selection is the fields used for "group-by", and the aggregation functions.
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(clgb.fields)+len(clgb.fns))
		for _, f := range clgb.fields {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	return selector.GroupBy(selector.Columns(clgb.fields...)...)
}

// CategoryLangSelect is the builder for selecting fields of CategoryLang entities.
type CategoryLangSelect struct {
	*CategoryLangQuery
	selector
	// intermediate query (i.e. traversal path).
	sql *sql.Selector
}

// Scan applies the selector query and scans the result into the given value.
func (cls *CategoryLangSelect) Scan(ctx context.Context, v interface{}) error {
	if err := cls.prepareQuery(ctx); err != nil {
		return err
	}
	cls.sql = cls.CategoryLangQuery.sqlQuery(ctx)
	return cls.sqlScan(ctx, v)
}

func (cls *CategoryLangSelect) sqlScan(ctx context.Context, v interface{}) error {
	rows := &sql.Rows{}
	query, args := cls.sql.Query()
	if err := cls.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// Modify adds a query modifier for attaching custom logic to queries.
func (cls *CategoryLangSelect) Modify(modifiers ...func(s *sql.Selector)) *CategoryLangSelect {
	cls.modifiers = append(cls.modifiers, modifiers...)
	return cls
}
